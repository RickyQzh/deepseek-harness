[package]
name = "dsh-token-meter"
version = "0.1.0"
description = "Replay-aware token measurement with a fixed four-characters-per-token heuristic for the DeepSeek Harness Rust host"
edition.workspace = true
license.workspace = true
publish.workspace = true
rust-version.workspace = true
repository.workspace = true

[dependencies]
dsh-boot = { path = "../dsh-boot" }
dsh-kernel = { path = "../dsh-kernel" }
dsh-llm = { path = "../dsh-llm" }
dsh-session = { path = "../dsh-session" }
serde_json = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }

[lints]
workspace = true
