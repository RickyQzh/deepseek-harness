[package]
name = "dsh-compaction"
version = "0.1.0"
description = "Compaction engine types, checkpoint source, and tool-pairing helpers for the DeepSeek Harness Rust host"
edition.workspace = true
license.workspace = true
publish.workspace = true
rust-version.workspace = true
repository.workspace = true

[dependencies]
dsh-agent-loop = { path = "../dsh-agent-loop" }
dsh-brand = { path = "../dsh-brand" }
dsh-session = { path = "../dsh-session" }
dsh-tools = { path = "../dsh-tools" }
serde = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }

[lints]
workspace = true
