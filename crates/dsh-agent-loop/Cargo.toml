[package]
name = "dsh-agent-loop"
version = "0.1.0"
description = "Scripted agent loop: inbox, phase machine, reconstruction, and tool scheduling for the Rust host"
edition.workspace = true
license.workspace = true
publish.workspace = true
rust-version.workspace = true
repository.workspace = true

[dependencies]
dsh-kernel = { path = "../dsh-kernel" }
dsh-llm = { path = "../dsh-llm" }
dsh-session = { path = "../dsh-session" }
dsh-system-prompt = { path = "../dsh-system-prompt" }
dsh-tools = { path = "../dsh-tools" }
futures = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
tokio = { workspace = true }

[lints]
workspace = true
