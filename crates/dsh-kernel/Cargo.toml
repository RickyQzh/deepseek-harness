[package]
name = "dsh-kernel"
version = "0.1.0"
description = "Fiber runtime, services, effects, isolate, and event bus for the DeepSeek Harness Rust host"
edition.workspace = true
license.workspace = true
publish.workspace = true
rust-version.workspace = true
repository.workspace = true

[dependencies]
thiserror = { workspace = true }
tokio = { workspace = true }

[lints]
workspace = true
