[package]
name = "dsh-tool-jobs"
version = "0.1.0"
description = "Model-facing job_output, job_list, and job_kill tools for the DeepSeek Harness Rust host"
edition.workspace = true
license.workspace = true
publish.workspace = true
rust-version.workspace = true
repository.workspace = true

[dependencies]
dsh-agent = { path = "../dsh-agent" }
dsh-agent-loop = { path = "../dsh-agent-loop" }
dsh-boot = { path = "../dsh-boot" }
dsh-jobs = { path = "../dsh-jobs" }
dsh-jobs-local = { path = "../dsh-jobs-local" }
dsh-kernel = { path = "../dsh-kernel" }
dsh-session = { path = "../dsh-session" }
dsh-tools = { path = "../dsh-tools" }
serde_json = { workspace = true }
tokio = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }

[lints]
workspace = true
