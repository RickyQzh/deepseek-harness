[package]
name = "dsh-terminal"
version = "0.1.0"
description = "Owner-scoped persistent PTY registry and in-memory pty-snapshot-backend for the DeepSeek Harness Rust host"
edition.workspace = true
license.workspace = true
publish.workspace = true
rust-version.workspace = true
repository.workspace = true

[dependencies]
dsh-boot = { path = "../dsh-boot" }
dsh-brand = { path = "../dsh-brand" }
dsh-kernel = { path = "../dsh-kernel" }
dsh-session = { path = "../dsh-session" }
serde_json = { workspace = true }
thiserror = { workspace = true }
tokio = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }

[lints]
workspace = true
