[package]
name = "dsh-compaction-basic"
version = "0.1.0"
description = "Log-only compaction lock, surface replace, and overflow retry for the DeepSeek Harness Rust host"
edition.workspace = true
license.workspace = true
publish.workspace = true
rust-version.workspace = true
repository.workspace = true

[dependencies]
dsh-agent-loop = { path = "../dsh-agent-loop" }
dsh-boot = { path = "../dsh-boot" }
dsh-compaction = { path = "../dsh-compaction" }
dsh-kernel = { path = "../dsh-kernel" }
dsh-llm = { path = "../dsh-llm" }
dsh-session = { path = "../dsh-session" }
dsh-token-meter = { path = "../dsh-token-meter" }
dsh-tools = { path = "../dsh-tools" }
futures = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
tokio = { workspace = true }

[dev-dependencies]
dsh-system-prompt = { path = "../dsh-system-prompt" }

[lints]
workspace = true
